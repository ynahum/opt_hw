import numpy as np

# 2.3
def grad_descent(x, Q, exact_line_search=True):
    x_k_list = []
    d = 0.5 * (Q + Q/T) @ x
    if exact_line_search:
        while 
        alpha = (x.T @ Q @ d) / (d.T @ Q @ d)

    return x_k_list
